import React from "react"
import { NavMenu } from "@/components/navmenu";

export const HomePage = () => {
    return (
        <div>
            <NavMenu />
            <h1>Home Page</h1>
        </div>
    )
}
